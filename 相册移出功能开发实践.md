【Code With SOLO】用 SOLO 构建照片管理平台，让相册移出功能"真正生效"

摘要：针对照片管理系统中"从相册移出"功能失效、MyBatis Plus 默认忽略 null 值字段导致 album_id 无法更新为 NULL 的痛点，我使用 TRAE SOLO 构建了一款本地化的照片管理 Web 平台。平台创新采用"原生 SQL + UpdateWrapper"的强制更新模式，并支持瀑布流布局与时间线分组展示，让照片管理既有视觉美感可浏览，又有逻辑强度不丢失数据。

1. 背景
我是来自一名开发者，在此，我带来我的作品：照片管理系统。
随着照片数量的增长，用户多少有着属于自己的整理需求，或是将照片按主题归类到相册中，或是从相册中临时移出某些照片。这些本都可以在相册详情页完成操作，可是，传统的"删除"功能会直接将照片移入回收站；如果想要只从相册中移除而保留在照片管理中，却发现点击"移出"后提示成功但照片依然显示在相册里，不免会让人感到困惑。
本项目通过"原生 SQL 注解 + 前端精简传参"的方式让相册移出功能真正生效，并且采用瀑布流布局配合时间线分组展示，保证用户的操作体验既直观又可靠，让小小操作不会失效。

2. 实践过程
以下是我的实现方式以及相关产品截图：

第一步：问题定位
首先我通过分析后端日志，发现 MyBatis Plus 的 updateById() 方法默认忽略 null 值字段：

```sql
-- 期望执行的 SQL
UPDATE t_photo SET album_id = NULL WHERE id = 17

-- 实际执行的 SQL  
UPDATE t_photo SET updated_at = '2026-04-13 19:45:27' WHERE id = 17
-- album_id 根本没有被更新！
```

第二步：初次尝试修复
然后，我尝试使用 UpdateWrapper 方式解决：

```java
// 尝试方案1：使用 UpdateWrapper
LambdaQueryWrapper<Photo> wrapper = new LambdaQueryWrapper<>();
wrapper.eq(Photo::getId, dto.getId());
Photo updatePhoto = new Photo();
updatePhoto.setAlbumId(null);
photoMapper.update(updatePhoto, wrapper);  // 失败！仍然忽略 null

// 尝试方案2：修改实体类字段策略
@TableField(strategy = FieldStrategy.IGNORED)
private Long albumId;  // 失败！Lombok 编译出错
```

第三步：踩坑与修正
在这里，我发现 MyBatis Plus 的所有 update 方法都会自动忽略 null 值字段，这是框架的设计行为而非 bug。最终决定采用**原生 SQL 注解**方式强制执行：

```java
@Mapper
public interface PhotoMapper extends BaseMapper<Photo> {
    @Update("UPDATE t_photo SET album_id = NULL WHERE id = #{photoId}")
    int removeAlbum(@Param("photoId") Long photoId);
}
```

同时简化前端传参，避免触发 hasOtherUpdates 逻辑：

```typescript
// 之前的代码（会触发错误逻辑）
await photoApi.update(photo.id, {
  id: photo.id,
  title: photo.title,        // 导致 hasOtherUpdates = true
  description: photo.description,
  albumId: null              // 条件判断失败，不执行更新
})

// 修复后的代码
await photoApi.update(photo.id, {
  id: photo.id,
  albumId: null   // 只传递需要更新的字段
})
```

第四步：最终结果
经过多轮调试，最终结果如下：

- **单张移出**：点击相册中照片的 X 图标 → 确认移出 → 照片从相册消失 → 照片仍保留在照片管理中
- **批量移出**：勾选多张照片 → 点击"从相册移除"按钮 → 批量移除成功
- **删除功能**：点击删除图标 → 照片移入回收站 → 从所有位置消失

核心代码变更文件：
- `PhotoMapper.java` - 新增 removeAlbum() 原生 SQL 方法
- `PhotoService.java` - 调用 removeAlbum() 替代 update()
- `AlbumDetail.vue` - 精简 API 调用参数

结语
以上就是我构建的大致流程，换作平时我需要去逐行调试 SQL 日志并查阅 MyBatis Plus 源码，而今使用本平台将排查时间从数小时缩短至了半小时。
因为本人学识有限，如有不通处，还望留下您宝贵的指导！
